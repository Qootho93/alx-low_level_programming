#include "main.h"
#include <stdlib.h>
/**
 * strtow - function that splits a string into words
 * @str: input
 * Return: pointer to concatenates string or NULL if str is NULL
 */
char **strtow(char *str)
{
	char **array;
	int i = 0, j, m, k = 0, len = 0, count - 0;

	if (str == NULL || *str == '\0')
		return (NULL);
	for (; str[i]; i++)
		
		
}
